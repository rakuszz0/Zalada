import { Server } from "socket.io"

async function LoginEvent(io: Server) {
    // Get user by claims token (parse it)   

    // Add to record if user not online

    // If already online but ip was different, add to array
}